# Test-only dependencies. Kept out of requirements.txt so the runtime image
# does not carry a test framework.
#
#   docker compose exec backend pip install -r backend/requirements-dev.txt
#   docker compose exec backend pytest
-r requirements.txt
pytest
httpx
