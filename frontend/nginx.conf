server {
    listen 80;
    server_name _;
    root /usr/share/nginx/html;
    index index.html;

    # Client-side routes like /dashboard have no file on disk, so fall back to
    # index.html and let React Router resolve them.
    location / {
        try_files $uri $uri/ /index.html;
    }
}
